import React from 'react'

function About(props) {
  return (
    <>
      <p>{props.id}</p>
      <p>{props.title}</p>
      <p>{props.location}</p>
      <image>{props.image}</image>
    </>
  )
}

export default About